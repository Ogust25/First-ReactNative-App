import { StyleSheet } from 'react-native'

const homeStyle = StyleSheet.create({
  body: {
    padding: 20,
  },
  title: {
    fontWeight: 'bold',
    fontSize: 30,
    textAlign: "center",
    marginBottom: 10,
  }
})

export default homeStyle
